SMS Alert System
========================
(under the root dir)

Prepare environment
> pip install -r requirements.txt

> pip install -e .

Start the system
> ./bin/system start

Stop the system
> ./bin/system stop

Test the system
> pytest
